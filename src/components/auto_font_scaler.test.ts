import { PolystatModel } from './types';
import { AutoFontScalar } from './auto_font_scaler';

describe('Test AutoFontScaler', () => {
  beforeEach(() => {

  });

  describe('Scale for Label Only', () => {
    it('returns font size for a labels', () => {
    });
  });

});
