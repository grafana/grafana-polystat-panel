name: Plugins - CI

on:
  push:
    branches:
      - main
  pull_request:

permissions: {}

jobs:
  ci:
    name: CI
    uses: grafana/plugin-ci-workflows/.github/workflows/ci.yml@e62d1bb2cac32d9de65197c7b11eb9f6010bfc14 # ci-cd-workflows/v7.2.0
    permissions:
      contents: read
      id-token: write
    with:
      plugin-version-suffix: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || '' }}
      run-playwright-with-skip-grafana-dev-image: true
      run-playwright-with-grafana-dependency: ">=10.1.0"
      upload-playwright-artifacts: true
